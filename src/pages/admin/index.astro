---
// /admin is now split into /admin/users and /admin/notes (linked from the
// Admin submenu in the nav). Keep this route as a permanent redirect so any
// existing /admin bookmarks still land somewhere useful.
return Astro.redirect("/admin/users", 302);
---
